# Complaint_Classifier
A FastAPI backend powered by a fine-tuned DistilBERT transformer model to classify customer complaints into categories with high accuracy. Includes analytics, batch processing, complaint storage, priority scoring, and health monitoring.
